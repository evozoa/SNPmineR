% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snp_map_ns.R
\name{snp_map_ns}
\alias{snp_map_ns}
\title{Map rsIDs to orthologous nonsynonymous variants (v0.1 scaffold)}
\usage{
snp_map_ns(
  rsids,
  targets = NULL,
  taxa = NULL,
  variant_db,
  ortholog_db,
  ref_db_targets = NULL,
  map_coords = FALSE,
  cache = TRUE,
  cache_dir = NULL,
  transcript_policy = c("canonical", "all", "pick_longest"),
  protein_coding_only = TRUE,
  one_to_many = c("keep_all", "best_hit", "drop_ambiguous"),
  min_score = NULL,
  format = c("long", "nested")
)
}
\arguments{
\item{rsids}{Character vector of rsIDs.}

\item{targets}{Character vector of target species codes (optional).}

\item{taxa}{A \code{TaxonSet} object (optional).}

\item{variant_db}{Human consequence table (e.g., VEP-derived). Must satisfy
\code{validate_variant_db()} schema.}

\item{ortholog_db}{Ortholog mapping table. Must satisfy \code{validate_ortholog_db()} schema.}

\item{ref_db_targets}{Optional reference database for target species (reserved for future use).}

\item{map_coords}{Logical; if TRUE, attach coordinate-mapping scaffold columns (requires \code{target_protein_id} for full support).}

\item{cache}{Logical; if TRUE, use the coordinate-mapping cache when \code{map_coords = TRUE}.}

\item{cache_dir}{Optional cache directory; defaults to a user cache directory via \code{tools::R_user_dir()}.}

\item{transcript_policy}{Transcript selection policy passed to \code{snp_rsids_to_consequence()}.}

\item{protein_coding_only}{Logical; passed to \code{snp_rsids_to_consequence()}.}

\item{one_to_many}{Policy for 1:many orthologs: "keep_all", "best_hit", "drop_ambiguous".}

\item{min_score}{Optional score threshold passed to \code{ortholog_resolve()} (if score column exists).}

\item{format}{Output format: "long" (default) or "nested".}
}
\value{
A tibble of class \code{SNPmineResult}.
}
\description{
This is the main user-facing entry point. In v0.1 it:
\enumerate{
\item filters a user-supplied human variant annotation table to rsIDs of interest
\item resolves target orthologs for implicated human genes
}
}
\details{
IMPORTANT: Taxonomic scope must be constrained by supplying either
\code{targets} or \code{taxa}. Global searches are not allowed.
}
